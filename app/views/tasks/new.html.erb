<% provide(:title, 'task new') %>
<h1>タスク新規作成</h1>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <%= form_with(model: @task, local: true) do |f| %>
      <%= render 'shared/error_messages2' %>
      
      <%= f.label :名称, class: "label-newtask" %>
      <%= f.text_area :task_name, class: "form-control", rows: 1 %>

      <%= f.label :詳細, class: "label-newtask" %>
      <%= f.text_area :note, class: "form-control", rows: 5 %>

      <%= f.submit "新規登録", class: "btn btn-primary btn-lg btn-newtask" %>
    <% end %>
  </div>
</div>