<% provide(:title, 'Tasks') %>
<h1>管理者のタスク一覧</h1>

<div class="main tasks-index">
  <div class="container">
    <% @tasks.each do |task| %>
    <div class="tasks-index-item">
      <%= task.task_name %>
      <%= task.note %>
    </div>
    <% end %>
  </div>
</div>